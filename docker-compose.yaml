version: '3.8'

services:
  agrobot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5017:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    volumes:
      - .:/app